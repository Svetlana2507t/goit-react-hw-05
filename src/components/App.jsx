import s from './App.module.css';

//import {Routes, Route } from 'react-router-dom';

function App() {
  return (
    <main>
      <h1 className={s.header}>Vite + React</h1>
      {/*  <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/movies" element={<MoviesPage />} />
        <Route path="*" element={<NotFoundPage />} />
      </Routes> */}
    </main>
  );
}

export default App;
